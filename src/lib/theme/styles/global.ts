/**
 * Global Styles
 *
 * CSS applied globally to the application.
 * These styles are injected by Chakra UI at the root level.
 */

export const globalStyles = {
  global: {
    body: {
      color: 'gray.400',
      fontFamily: 'var(--font-degular)',
    },
  },
}
